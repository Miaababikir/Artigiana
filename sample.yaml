models:
  User:
    name: string
    email: string
    password: string

  Post:
    title: string
    content: text
    user_id: foreignId
    published_at: timestamp

controllers:
  Post:
    index:
      query: all
      render: post.index with:posts